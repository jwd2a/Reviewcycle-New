# Hurl configuration file
#
# Set default variables here or via command line with --variable
# These are used by all .hurl files in this directory

# Default API URL (production)
api_url=https://reviewcycle-api.onrender.com

# For local development, uncomment:
# api_url=http://localhost:3000

# Set these via command line or create a .env file:
# admin_key=your_admin_key_here
# api_key=rc_proj_demo123

# Default project name
project_name=New Project
